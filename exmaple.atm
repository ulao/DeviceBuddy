; Automate code Version 1.0
[1.0]

when
{
	currentController = gamecube
	and
	currentControllerOLD !=  gamecube ;this prevent constant writing, so that it does so only on startup and chhange
}

do
{
	;API_WRITE->0x11(0x01, 0x02, 0xFF)
	;API_READ->0x11 
	RANGE(-95,95)
	;RUMBLE
}

when
{
	;dummy
}

do
{
	;dummy
}

when
{
	;dummy
}

do
{
	;dummy
}

